import org.junit.Test;
import static org.junit.Assert.*;

public class HelloTest  {
    @Test
    public void testHello() {
        assertEquals(2, 1 + 1);
    }
}
//greetings from cloudwave technologies
// This is a simple test class to verify that the testing framework is set up correctly.